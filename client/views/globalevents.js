/**
 * Global events that are not related to a specific template.
 *
 * Requires package jquery
 */
Meteor.startup(function() {

    // declare standard jQuery events here
    /*
    $(document).ready(function() {
        $('selector').on('click', function() {

        });
    });
    */
});